// You can't define a variable more than once.
let petName = 'Hal';
petName = 'Spot';

console.log(petName);

// There are rules related to variable names. 
// You can't start a variable with a number.
let test_ = 2;
let result = 3 + 4;

// Variable names cannot be reserved keywords

/* The var keyword (Not advised)
    * Only function scoped
    * Can be declared multiple times without breaking the code.
    * All declarations are hoisted up
*/